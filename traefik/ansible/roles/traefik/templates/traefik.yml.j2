entryPoints:
  http:
    address: ":80"
  https:
    address: ":443"
  proxmox:
    address: ":{{ proxmox.traefik_port }}"

api:
  dashboard: true

providers:
  
  docker:
    endpoint: "unix:///var/run/docker.sock"

  file:
    filename: "/etc/traefik/dynamic.yml"
